<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
	Identical to Info.plist, plus one key: NSAllowsLocalNetworking.

	It is what lets `MINDLOG_ENV=local` reach http://localhost:8080 — App
	Transport Security refuses cleartext otherwise. It permits local addresses
	only, never an arbitrary host, and it is confined to the debug
	configurations: the release build uses Info.plist and therefore the platform
	default, HTTPS only.

	Same arrangement as the Android client, which carries its
	networkSecurityConfig in `app/src/debug/AndroidManifest.xml` and nowhere
	else. Two files that must be kept in step is the price; the alternative is
	shipping the exception to production.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>$(MINDLOG_APP_NAME)</string>
	<key>CFBundleShortVersionString</key>
	<string>0.1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>

	<key>MindlogEnv</key>
	<string>$(MINDLOG_ENV)</string>

	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>

	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			</array>
		</dict>
	</array>

	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
