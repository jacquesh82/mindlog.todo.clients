<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext for the local development server only, and only in debug builds.

  Deliberately not `usesCleartextTraffic="true"`: that would permit plain HTTP
  to any host, in every build. Scoping it here means a release build is
  structurally incapable of talking in the clear, rather than merely unlikely
  to.

  10.0.2.2 is the host machine as seen from the Android emulator.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
