<?xml version="1.0" encoding="utf-8"?>
<!--
  The session store holds a refresh token encrypted with a non-exportable
  Android Keystore key. Restoring it onto another device would yield a blob
  nothing can decrypt, so it is excluded rather than shipped uselessly.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </device-transfer>
</data-extraction-rules>
